language: julia
os:
  - linux
  - osx
julia:
  - 0.6
  - nightly
matrix:
    allow_failures:
        - julia: nightly
notifications:
  email: false
sudo: false
#script:
#  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
#  - julia -e 'Pkg.clone(pwd()); Pkg.build("SCS"); Pkg.test("SCS"; coverage=true)'
before_script:
  - julia -e 'Pkg.add("MathOptInterface"); Pkg.checkout("MathOptInterface")' # TODO remove when MOI v0.3 is released
after_success:
- julia -e 'cd(Pkg.dir("SCS")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(Coveralls.process_folder())'
